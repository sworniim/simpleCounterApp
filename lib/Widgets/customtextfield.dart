import 'package:flutter/material.dart';

class CustomTextField extends StatelessWidget {
  final IconData? prefixIcon;
  final String title;
  final String hinter;

  const CustomTextField({Key? key,required this.hinter,required this.title,this.prefixIcon}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
